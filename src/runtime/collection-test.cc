#include "runtime/collection.h"
#include "gtest/gtest.h"

class CollectionTest : public ::testing::Test {

};

TEST_F(CollectionTest, Sanity) {
//    YALX_ARRAY_TY(int) ints = YALX_ARRAY(4, int);
//    ASSERT_TRUE(ints != nullptr);
//    ASSERT_EQ(4, yalx_array_len(ints));
//    ASSERT_EQ(8, yalx_array_capacity(ints));
}
